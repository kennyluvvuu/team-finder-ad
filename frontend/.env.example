# --- Frontend Environment Variables ---
# URL of the Django backend API. 
# Defaults to http://127.0.0.1:8000 when running locally, or http://backend:8000 in Docker.
BACKEND_URL=http://127.0.0.1:8000
